/*
 * Host tests use the real distributed module and the project headers.
 * No kernel symbols are needed because distributed.c uses fixed-size state
 * and does not allocate dynamically.
 */
int nexora_phase9_host_stubs_translation_unit = 0;
