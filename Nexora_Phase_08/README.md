# Nexora Phase 08 — provenance marker

The original Phase 08 source/archive is **not present in the repository history available to this consolidation**.

This directory is intentionally a provenance marker rather than reconstructed code. Fabricating a Phase 08 snapshot would make the development lineage less trustworthy. The next preserved code lineage is Phase 09, whose `reference_tree/` contains the baseline carried forward at that point.

If the original Phase 08 archive is recovered later, it should be added here with:

- its original README/specification;
- source files exactly as recovered;
- a cryptographic manifest;
- the commit/source provenance used to establish authenticity.

For current development, use the authoritative top-level `kernel/`, `bench/`, `qualification/`, and `release/` trees.
